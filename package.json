{
  "name": "maktaba-platform",
  "private": true,
  "version": "1.0.0",
  "description": "منصة المكتبة الإلكترونية — واجهة React وخادم Express مع قاعدة بيانات Supabase",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "node scripts/dev.mjs",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix server && npm run build --prefix client",
    "start": "npm start --prefix server",
    "create-admin": "npm run create-admin --prefix server",
    "typecheck": "npm run typecheck --prefix server && npm run typecheck --prefix client",
    "test": "npm test --prefix server"
  },
  "engines": { "node": ">=22.13" }
}
